# 🚀 VM4 - Упрощенная конфигурация

## Описание
Простая и надежная конфигурация для развертывания одной виртуальной машины в Proxmox.

## Особенности
- ✅ Монолитная конфигурация без модулей
- ✅ Уникальные имена файлов (без конфликтов)
- ✅ Полная автоматизация с cloud-init
- ✅ SSH доступ с ключами
- ✅ Базовый набор пакетов
- ✅ Сетевые настройки

## Параметры VM
- **OS**: Ubuntu 22.04 LTS
- **CPU**: 2 cores
- **RAM**: 4GB
- **Disk**: 50GB
- **IP**: 192.168.123.140
- **VM ID**: 740

## Команды

### Быстрый старт с Makefile
```bash
# Показать все доступные команды
make help

# Полный цикл: развертывание + тестирование
make deploy-and-test

# Быстрое развертывание
make quick-deploy

# SSH тестирование
make test-ssh

# Пересоздание VM
make recreate
```

### Ручные команды Terraform
```bash
# Инициализация
terraform init

# Проверка плана
terraform plan

# Развертывание
terraform apply

# Подключение
ssh devops@192.168.123.140

# Удаление
terraform destroy
```

### SSH Тестирование
```bash
# Запустить полный SSH тест
./test-ssh.sh

# Быстрая проверка доступности
ping 192.168.123.140

# Проверка SSH порта
nc -zv 192.168.123.140 22
```

## Структура проекта
```
vm4/
├── main.tf           # Основная конфигурация
├── cloud-init.tftpl  # Шаблон cloud-init  
├── terraform.tfvars  # Переменные
├── outputs.tf        # Outputs
├── test-ssh.sh       # SSH тестирование
├── Makefile          # Автоматизация команд
└── README.md         # Документация
```

## Тестирование

### Автоматическое тестирование
VM4 включает встроенное автоматическое тестирование:

1. **Network Test** - проверка доступности по ping
2. **SSH Port Test** - проверка открытого SSH порта  
3. **SSH Connection Test** - проверка подключения с ключами/паролем
4. **Basic Commands Test** - выполнение базовых команд
5. **Cloud-init Test** - проверка результатов cloud-init
6. **Packages Test** - проверка установленных пакетов

### Запуск тестов
```bash
# Запустить все тесты
./test-ssh.sh

# Или через Makefile
make test-ssh
```

## Безопасность
- SSH доступ только по ключам
- Firewall включен по умолчанию
- Пользователь devops с sudo правами
- Пароли в переменных помечены как sensitive